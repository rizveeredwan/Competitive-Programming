#include <bits/stdc++.h>
#define MAX_SIZE 1000000
using namespace std;


struct Point {
    double x,y;
};

Point P[20];
Pointe temp[20];

void merge_sort() {
    // left
    mid = (st+en)/2;
    merge_sort(P, st, mid);
    merge_sort(mid+11, en);
    i = st, j = mid, k=0;
    while

}
